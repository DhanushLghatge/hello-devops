trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: Build
  jobs:
  - job: BuildJob
    steps:
    - script: echo "Building application..."

- stage: Deploy
  jobs:
  - job: DeployJob
    steps:
    - script: echo "Deploying application..."
    - script: echo "App deployed successfully 🚀"